<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/header_bar_size"
    android:background="@drawable/daohanglan_img" >

    <ImageButton
        android:id="@+id/id_btn_left"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:background="@drawable/gerenzhongxin_shoucang"
        android:contentDescription="@string/img_content_description"
        android:onClick="showLeftMenu"
        android:visibility="invisible" />

    <ImageButton
        android:id="@+id/id_btn_right"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dp"
        android:background="@drawable/sousuo_sousuo"
        android:contentDescription="@string/img_content_description"
        android:onClick="showSearchWindow"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/id_txt_title"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/id_btn_right"
        android:layout_toRightOf="@+id/id_btn_left"
        android:gravity="center"
        android:text="@string/title_toutiao"
        android:textColor="#fff"
        android:textSize="@dimen/title_size"
        android:textStyle="bold"
        android:visibility="invisible" />

</RelativeLayout>